{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Mon profil</h1>

    {{ include('admin/_form.html.twig', {'button_label': 'Modifier','etat': 'warning'}) }}
    <div class="row d-flex justify-content-between m-1">
        <div class="col-sm-4">
            <a class="btn btn-primary" href="{{ path('app_admin_index') }}">Retour <i class="fa fa-backward"></i></a>
        </div>
        <div class="col-sm-4">
            {# {{ include('admin/_delete_form.html.twig') }} #}
        </div>
    </div>
{% endblock %}
